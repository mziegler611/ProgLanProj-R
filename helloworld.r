print("Hello world")
#read a csv file, and interpret the data
#functions
    #simple ones
        #print out the header(s)
        #print out the data
    #less simple ones
        #find the frequency of a result in a column
        #find the average of a column if we are looking at numeric data
        #find max or most occuring of a column  
        #find min or least occuring of a column
        #replace results in a column with another things (like for the dog data set: replace all instances of boxers with pugs)
